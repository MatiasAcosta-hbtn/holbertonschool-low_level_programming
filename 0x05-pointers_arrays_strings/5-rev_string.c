#include "holberton.h"
#include <stdio.h>
/**
 * _puts - prints a text
 * @str: the text received
 */

void rev_string(char *s)
{
	int i = 0, j=0;
	printf("%lu,",sizeof(s));

/*	while (*(s + i) != 0)
		i++;
	char arr[i];

	for (i = i - 1; i >= 0; i--)
	{
		arr[j] = *(s + i);
		j++;
	}
	
	for(
	_putchar(10);*/
}

int main(void){

char s[10];

rev_string(s);

}
