#include "holberton.h"
#include <stdio.h>
/**
 * puts2 - prints a text
 * @str: the string received
 */

void print_array(int *a, int n)
{
	int i = 0;
	int arr[n] = *a;

/*	for (i = 0; i < n; i++)
		printf("%d, ", arr[i]);	 */
}
