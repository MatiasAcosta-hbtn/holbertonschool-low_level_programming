#include "holberton.h"
/**
 * mul - return the multiplication of two numbers
 * @a: the first number
 * @b: the second number
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
